<h2>ログイン</h2>

<%= form_for(resource, as: resource_name, url: products_path(resource_name)) do |f| %>
　<div>会員の方はこちらからログイン</div>
  <div class="field">
    <%= f.label "メールアドレス" %><%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label "パスワード" %><%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "customers/shared/links" %>


<div>会員登録がお済みでない方</div>
<div class="create-user-button"><%= link_to "こちら", new_customer_registration_path %>　から新規登録を行ってください。</div>
